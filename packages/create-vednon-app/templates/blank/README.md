# __PROJECT_NAME__

A blank [Vednon](../../) workspace.

## Open it

From the Vednon repo root:

```bash
./scripts/code.sh __PROJECT_NAME__
```

This opens the folder in the de-deved Vednon window — Explorer, Search, and Extensions only,
with the full extension host and MCP available.

## Configuration

`vednon.json` holds the workspace configuration:

```json
{ "name": "__PROJECT_NAME__" }
```

Add your artifacts (markdown, JSON, etc.) anywhere in this folder.
